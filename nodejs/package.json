{
  "name": "file-encoding-detector",
  "version": "1.0.0",
  "description": "Detect the character encoding of every file in a folder.",
  "type": "module",
  "bin": {
    "detect-encoding": "./bin/detect.js"
  },
  "main": "./lib/detector.js",
  "scripts": {
    "start": "node bin/detect.js",
    "test": "vitest run tests/"
  },
  "keywords": [
    "encoding",
    "charset",
    "detection",
    "cli",
    "files"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "chardet": "^2.0.0",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
